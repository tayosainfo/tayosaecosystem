version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: tayosa-postgres
    environment:
      POSTGRES_DB: tayosa_banking
      POSTGRES_USER: tayosa
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - tayosa-network

volumes:
  postgres_data:

networks:
  tayosa-network:
    driver: bridge
